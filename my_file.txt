Today i work with git as a trainee

git config ? 
git config is the primary command used to set and view variables that control how Git looks and operates.
example  git config --global user.name "Your Name"
         git config --global user.email "you@example.com"


git pull? 
i understand git pull command , it fetch data and merge also.
example : i have two branch main and newbranch so i pull data in newbranch from main.
git switch newbranch
git pull origin main
git push origin newbranch


git fetch?
Use git fetch if you want to see what others have been working on without disrupting your current progress. After fetching, you can use git log or git diff to review the incoming changes before manually running git merge.
